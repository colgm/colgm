/* must use optimization level >= 1 */

use std::panic::{ assert };

/*
  expect to be:
    define i64 @get() {
      label.L2:	; bb.entry
      ret i64 0
    }
*/
func get() -> i64 {
    return (2 + 2) - (2 * 1 * 1 * 1 * 2);
}

func main() -> i64 {
    assert(get() == 0, "get() != 0");
    return 0;
}