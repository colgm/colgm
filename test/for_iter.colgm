use std::vec::{ primitive_vec };
use std::io::{ io };

pub func main() -> i32 {
    io::stdout().out("[for_iter.colgm] testing").endln();
    var a = primitive_vec<i32>::instance();
    a.push(1);
    a.push(2);

    forindex (var i; a) {}

    foreach (var i; a) {}

    a.delete();
    return 0;
}