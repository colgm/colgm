struct vec_push {
    pu: i32,
    sh: i32
}

impl vec_push {
    pub func push(self) {}
    pub func test() {
        var vp = vec_push {};
        vp.push_back();
    }
}

struct vec_push_back {
    pu: i32,
    sh: i32,
    ba: i32,
    ck: i32
}

impl vec_push_back {
    pub func push_back(self) {}
    pub func test() {
        var vpb = vec_push_back {};
        vpb.push();
    }
}

func main() -> i32 {
    vec_push::test();
    vec_push_back::test();
    return 0;
}