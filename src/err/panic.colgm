use std::libc::{ write, exit, strlen };

pub func panic(info: i8*) {
    var panic_head = "Colgm panic: ";
    write(2 => i32, panic_head, strlen(panic_head));
    write(2 => i32, info, strlen(info));
    write(2 => i32, "\n", 1);
    exit(-1 => i32);
}