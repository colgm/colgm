use ast::ast::*;
use err::report::*;
use lexer::{ lexer };

struct parser {
    err: report*,
    lex: lexer*
}

impl parser {
    func new(e: report*, l: lexer*) -> parser* {
        var res = parser::__alloc__();
        res->err = e;
        res->lex = l;
        return res;
    }

    func delete(self: parser*) {
        return;
    }
}

impl parser {
    func parse(self: parser*) {
        # TODO
        return;
    }
}