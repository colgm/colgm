use std::str::str;
use std::libc::{malloc, print, println};
use err::span::span;
use ast::ast::*;
use cli::*;

func main(argc: i32, argv: i8**) -> i32 {
    help();
    logo();
    return i64::i32(0);
}