use std::libc::{ close };

pub enum af_domain {
    AF_INET = 2
}

pub enum ip_proto {
    IPPROTO_IP = 0,
    IPPROTO_ICMP = 1,
    IPPROTO_TCP = 6,
    IPPROTO_UDP = 17
}

extern pub func socket(domain: i32, type: i32, protocal: i32) -> i32;
