use std::str::{ str };
use err::report::{ report };
use std::io::{ io };

struct str_hashmap {

}

struct package {
    err: report*
}

impl package {
    func new(e: report*) -> package* {
        var res = package::__alloc__();
        res->err = e;
        return res;
    }

    func delete(self) {
        io::stderr().out("[").green().out("package").reset().out("]")
                    .out(" delete package\n");
    }
}